import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 24,
    },
    list: {
        marginVertical: 24,
    },
    productsList: {
        // paddingHorizontal: 5,
    }
})

export default styles;